# 项目维护入口

## 开始工作

1. 先核对 `git status --short --branch`、近期提交和 `src/__init__.py`，保留已有工作区改动。
2. 阅读 `docs/maintenance.md` 获取当前维护范围、验证方法、已知限制和交接要求。
3. 按任务阅读 `README.md`（使用部署）、`docs/architecture.md`（架构）、`docs/protocol.md`（传输契约）。
4. `docs/superpowers/` 是历史设计和执行证据，不是待自动执行的任务队列；历史复选框不代表当前任务状态。

## 必须保持的边界

- 维护方向是 OpenCode V2、原生多 Server、浏览器 P2P 优先和 Relay 兜底。
- Gateway 是入口和传输服务；业务请求属于明确设备。设备显示名不代表身份。
- 保持请求体字节与文本/二进制帧语义；代理重新分帧时剥离逐跳头。
- 结果未知的已发 mutation 不自动重放到其他传输；异步操作固定其原设备和通道。
- V2 启动适配依赖已验证的入口契约；上游升级需要真实浏览器复验。
- 用户数据、认证配置和本地运行证据均与公开源码分离；不要清空数据库或浏览器存储来掩盖问题。

## 修改与验证

- 先读取目标文件，优先已有技术栈；让改动与本次任务范围一致。
- 注释解释约束和原因，避免复述代码或保留会话式调试叙述。
- 功能修改提供针对实际行为的回归验证；测试和浏览器验收方法见 `docs/maintenance.md`。
- 文档和测试示例使用虚构设备、文档专用域名/IP。真实设备、账号、地址及连接凭据留在忽略的本地资料中。
- 提交、推送和部署依据当前任务授权执行；成功声明需有对应命令或浏览器证据。

## 结束与交接

- 更新受影响的当前文档，按维护页模板记录剩余工作、验证结果和关键决定。
- 未复现、未验证、外部服务限制要分别说明；不要把单元测试通过当作浏览器全链路通过。
- 新会话应能从仓库恢复工作，不依赖聊天摘要、临时目录或特定 AI 的记忆服务。
